// An extendable class which handles converting the format recursively to a usable JSON format.
// You determine which keys you export and in what order.
export default class Exportable
{
	export()
	{
		console.log(5);
	}
}